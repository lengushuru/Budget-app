<%# <div class="category_head">
<h2>My Categories</h2>
<a href="/logout">
<i style='font-size:24px' class='fas'>&#xf2f5;</i>
</a>
</div> %>
 <nav class="navbar navbar-expand-md navbar-light bg-3778c2 ">
     <button class="navbar-toggler" data-toggle="collapse" data-target="#collapsibleNavbar">
            <i class="fa-solid fa-bars" id="mobile-menu"></i>
        </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                <a class="nav-link"id="logout" href="/logout">Logout</a>
                </li>
            </ul>
        </div>
        <!-- Brand/logo -->
        <a class="navbar-brand text-white" href="#">CARTEGORIES</a>
        
        <!-- Mobile Menu with Navbar Text and Magnifying Glass Icon -->
        <div class="d-md-none d-flex align-items-center mr-auto m-2">
            <i class="fas fa-search" id="search-icon"></i>
        </div>

        <!-- Toggler/collapsible Button -->
        
        
        <!-- Navbar links -->
     
    </nav>

 <div>
    <% if @categories.nil? %>
      <h3>Add a Category</h3>
    <% else %>
    <% @categories.each do |category| %>
   <a href="<%= category_payments_path(category.id) %>">
    <div class = "category">
    <img src= "<%= category.icon %>" alt="icon" calss="icon">
    <p><%= category.name %></p>
    <p> Total: <%= category.total_transaction_amount%>$</p>
    </div>
    <% end %>
    <% end %>
     <div class = "footer_container">
     <a href="/categories/new">
        <div class = "footer">
            <p>Add Category</p>
            <i class="fa-solid fa-plus"></i>
        </div>
     </a>
    </div>
  </div>

